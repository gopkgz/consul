name: main

on:
  push:
    branches:
    tags:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    - name: Go Lint
      uses: samuelmeuli/lint-action@v1.2.0
      with:
        auto_fix: n
    - name: Shell Linter
      uses: azohra/shell-linter@v0.2.0
